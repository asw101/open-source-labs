{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "metadata": {
    "_generator": {
      "name": "bicep",
      "version": "0.46.1.21595",
      "templateHash": "147430417931669304"
    }
  },
  "parameters": {
    "vmName": {
      "type": "string",
      "defaultValue": "vm1",
      "metadata": {
        "description": "The name of your Virtual Machine."
      }
    },
    "vmSize": {
      "type": "string",
      "defaultValue": "Standard_B2s_v2",
      "allowedValues": [
        "Standard_B2ts_v2",
        "Standard_B2ls_v2",
        "Standard_B2s_v2",
        "Standard_B4ls_v2",
        "Standard_B4s_v2",
        "Standard_D2s_v5",
        "Standard_D4s_v5",
        "Standard_D2ps_v5",
        "Standard_D4ps_v5"
      ],
      "metadata": {
        "description": "The Virtual Machine size."
      }
    },
    "vmSizeCustom": {
      "type": "string",
      "defaultValue": "",
      "metadata": {
        "description": "Custom VM size (overrides vmSize if set)."
      }
    },
    "diskAccountType": {
      "type": "string",
      "defaultValue": "Premium_LRS",
      "allowedValues": [
        "Standard_LRS",
        "Premium_LRS"
      ],
      "metadata": {
        "description": "The Storage Account Type for OS and Data disks."
      }
    },
    "osDiskSize": {
      "type": "int",
      "defaultValue": 64,
      "allowedValues": [
        1024,
        512,
        256,
        128,
        64,
        32
      ],
      "metadata": {
        "description": "The OS Disk size."
      }
    },
    "osImage": {
      "type": "string",
      "defaultValue": "Ubuntu 24.04-LTS",
      "allowedValues": [
        "Ubuntu 24.04-LTS",
        "Ubuntu 24.04-LTS (arm64)"
      ],
      "metadata": {
        "description": "The OS image for the VM."
      }
    },
    "location": {
      "type": "string",
      "defaultValue": "[resourceGroup().location]",
      "metadata": {
        "description": "Location for all resources."
      }
    },
    "adminUsername": {
      "type": "string",
      "defaultValue": "azureuser",
      "metadata": {
        "description": "Username for the Virtual Machine."
      }
    },
    "adminPasswordOrKey": {
      "type": "securestring",
      "defaultValue": "",
      "metadata": {
        "description": "SSH Key for the Virtual Machine."
      }
    },
    "cloudInit": {
      "type": "string",
      "defaultValue": "none",
      "allowedValues": [
        "none",
        "tailscale"
      ],
      "metadata": {
        "description": "Deploy with Tailscale SSH."
      }
    },
    "env": {
      "type": "secureObject",
      "defaultValue": {},
      "metadata": {
        "description": "Environment variables as JSON object (e.g. {\"tskey\":\"tskey-auth-...\"})."
      }
    }
  },
  "variables": {
    "rand": "[substring(uniqueString(resourceGroup().id), 0, 6)]",
    "vnetName": "[format('{0}-vnet', resourceGroup().name)]",
    "subnetName": "default",
    "keyData_var": "[if(not(equals(parameters('adminPasswordOrKey'), '')), parameters('adminPasswordOrKey'), 'ssh-rsa ')]",
    "publicIPAddressName": "[format('{0}-ip', parameters('vmName'))]",
    "networkInterfaceName": "[format('{0}-nic', parameters('vmName'))]",
    "ipConfigName": "[format('{0}-ipconfig', parameters('vmName'))]",
    "subnetAddressPrefix": "10.1.0.0/24",
    "addressPrefix": "10.1.0.0/16",
    "cloudInitTailscale": "#cloud-config\n# vim: syntax=yaml\n\npackages:\n- jq\n- curl\n\nwrite_files:\n- path: /home/azureuser/env.json\n  content: __ENV_B64__\n  encoding: b64\n- path: /usr/local/bin/tailscale-authurl\n  permissions: '0755'\n  content: |\n    #!/usr/bin/env bash\n    # Print this node's Tailscale interactive login URL.\n    # Pass --refresh to discard an expired URL and mint a fresh one.\n    set -eo pipefail\n\n    if [ \"$#\" -gt 0 ] && [ \"$1\" = \"--refresh\" ]; then\n        tailscale logout >/dev/null 2>&1 || true\n        setsid tailscale up --ssh --timeout=1s >/dev/null 2>&1 || true\n    fi\n\n    for i in $(seq 1 30); do\n        url=$(tailscale status --json 2>/dev/null | jq -r '.AuthURL // empty')\n        if [ -n \"$url\" ]; then\n            echo \"$url\"\n            exit 0\n        fi\n        state=$(tailscale status --json 2>/dev/null | jq -r '.BackendState // empty')\n        if [ \"$state\" = \"Running\" ]; then\n            echo \"already onboarded; re-run with --refresh to re-authenticate\" >&2\n            exit 0\n        fi\n        sleep 2\n    done\n\n    echo \"no auth URL available\" >&2\n    exit 1\n- path: /home/azureuser/tailscale.sh\n  content: |\n    #!/usr/bin/env bash\n    # Install Tailscale, then either join with an auth key or publish a login URL.\n    set -eo pipefail\n\n    curl -fsSL https://tailscale.com/install.sh | sh\n\n    key=\"$1\"\n    if [ -n \"$key\" ] && [ \"$key\" != \"null\" ]; then\n        tailscale up --ssh --authkey \"$key\"\n        # Console writes are best-effort: never fail the boot over logging.\n        echo \"tailscale: joined tailnet with auth key\" >/dev/console 2>/dev/null || true\n        exit 0\n    fi\n\n    # No auth key. 'tailscale up' blocks until the node is authorized, so run it\n    # detached and poll the daemon for the login URL it generates.\n    setsid tailscale up --ssh >/var/log/tailscale-up.log 2>&1 &\n\n    url=\"\"\n    for i in $(seq 1 60); do\n        url=$(tailscale status --json 2>/dev/null | jq -r '.AuthURL // empty')\n        if [ -n \"$url\" ]; then\n            break\n        fi\n        sleep 2\n    done\n\n    if [ -z \"$url\" ]; then\n        echo \"tailscale: FAILED to obtain a login URL\" >/dev/console 2>/dev/null || true\n        echo \"tailscale: FAILED to obtain a login URL\" >&2\n        exit 1\n    fi\n\n    echo \"$url\" >/var/lib/tailscale-authurl.txt\n    chmod 0644 /var/lib/tailscale-authurl.txt\n\n    # Publish to the serial console so it reaches Azure boot diagnostics.\n    # Best-effort: the file above is the authoritative copy.\n    printf '\\n===== TAILSCALE LOGIN URL =====\\n%s\\n===============================\\n\\n' \\\n        \"$url\" >/dev/console 2>/dev/null || true\n\nruncmd:\n- cd /home/azureuser/\n- bash tailscale.sh \"$(jq -r '.tskey' env.json)\"\n- echo $(date) > hello.txt\n- chown -R azureuser:azureuser /home/azureuser/\n",
    "cloudInitTailscaleFormat": "[replace(variables('cloudInitTailscale'), '__ENV_B64__', base64(string(parameters('env'))))]",
    "kvCloudInit": {
      "none": null,
      "tailscale": "[base64(variables('cloudInitTailscaleFormat'))]"
    },
    "kvImageReference": {
      "Ubuntu 24.04-LTS": {
        "publisher": "canonical",
        "offer": "ubuntu-24_04-lts",
        "sku": "server",
        "version": "latest"
      },
      "Ubuntu 24.04-LTS (arm64)": {
        "publisher": "canonical",
        "offer": "ubuntu-24_04-lts",
        "sku": "server-arm64",
        "version": "latest"
      }
    },
    "nsgSecurityRules": [
      {
        "name": "Port_41641",
        "properties": {
          "protocol": "Udp",
          "sourcePortRange": "*",
          "destinationPortRange": "41641",
          "sourceAddressPrefix": "Internet",
          "destinationAddressPrefix": "*",
          "access": "Allow",
          "priority": 100,
          "direction": "Inbound"
        }
      }
    ]
  },
  "resources": [
    {
      "type": "Microsoft.ManagedIdentity/userAssignedIdentities",
      "apiVersion": "2023-01-31",
      "name": "[format('{0}-identity', resourceGroup().name)]",
      "location": "[parameters('location')]"
    },
    {
      "type": "Microsoft.Network/networkSecurityGroups",
      "apiVersion": "2023-11-01",
      "name": "[format('{0}-nsg', resourceGroup().name)]",
      "location": "[parameters('location')]",
      "properties": {
        "securityRules": "[variables('nsgSecurityRules')]"
      }
    },
    {
      "type": "Microsoft.Network/virtualNetworks",
      "apiVersion": "2023-11-01",
      "name": "[variables('vnetName')]",
      "location": "[parameters('location')]",
      "properties": {
        "addressSpace": {
          "addressPrefixes": [
            "[variables('addressPrefix')]"
          ]
        },
        "subnets": [
          {
            "name": "[variables('subnetName')]",
            "properties": {
              "addressPrefix": "[variables('subnetAddressPrefix')]"
            }
          }
        ]
      }
    },
    {
      "type": "Microsoft.Network/publicIPAddresses",
      "apiVersion": "2023-11-01",
      "name": "[variables('publicIPAddressName')]",
      "location": "[parameters('location')]",
      "sku": {
        "name": "Standard"
      },
      "properties": {
        "publicIPAllocationMethod": "Static",
        "dnsSettings": {
          "domainNameLabel": "[toLower(format('{0}-{1}', parameters('vmName'), variables('rand')))]"
        }
      }
    },
    {
      "type": "Microsoft.Network/networkInterfaces",
      "apiVersion": "2023-11-01",
      "name": "[variables('networkInterfaceName')]",
      "location": "[parameters('location')]",
      "properties": {
        "ipConfigurations": [
          {
            "name": "[variables('ipConfigName')]",
            "properties": {
              "subnet": {
                "id": "[reference(resourceId('Microsoft.Network/virtualNetworks', variables('vnetName')), '2023-11-01').subnets[0].id]"
              },
              "privateIPAllocationMethod": "Dynamic",
              "publicIPAddress": {
                "id": "[resourceId('Microsoft.Network/publicIPAddresses', variables('publicIPAddressName'))]"
              }
            }
          }
        ],
        "networkSecurityGroup": {
          "id": "[resourceId('Microsoft.Network/networkSecurityGroups', format('{0}-nsg', resourceGroup().name))]"
        }
      },
      "dependsOn": [
        "[resourceId('Microsoft.Network/networkSecurityGroups', format('{0}-nsg', resourceGroup().name))]",
        "[resourceId('Microsoft.Network/publicIPAddresses', variables('publicIPAddressName'))]",
        "[resourceId('Microsoft.Network/virtualNetworks', variables('vnetName'))]"
      ]
    },
    {
      "type": "Microsoft.Compute/virtualMachines",
      "apiVersion": "2024-03-01",
      "name": "[parameters('vmName')]",
      "location": "[parameters('location')]",
      "identity": {
        "type": "UserAssigned",
        "userAssignedIdentities": {
          "[format('{0}', resourceId('Microsoft.ManagedIdentity/userAssignedIdentities', format('{0}-identity', resourceGroup().name)))]": {}
        }
      },
      "properties": {
        "hardwareProfile": {
          "vmSize": "[if(not(equals(parameters('vmSizeCustom'), '')), parameters('vmSizeCustom'), parameters('vmSize'))]"
        },
        "storageProfile": {
          "osDisk": {
            "managedDisk": {
              "storageAccountType": "[parameters('diskAccountType')]"
            },
            "name": "[format('{0}-osdisk1', parameters('vmName'))]",
            "diskSizeGB": "[parameters('osDiskSize')]",
            "createOption": "FromImage"
          },
          "imageReference": "[variables('kvImageReference')[parameters('osImage')]]"
        },
        "diagnosticsProfile": {
          "bootDiagnostics": {
            "enabled": true
          }
        },
        "networkProfile": {
          "networkInterfaces": [
            {
              "id": "[resourceId('Microsoft.Network/networkInterfaces', variables('networkInterfaceName'))]"
            }
          ]
        },
        "osProfile": {
          "computerName": "[parameters('vmName')]",
          "customData": "[variables('kvCloudInit')[parameters('cloudInit')]]",
          "adminUsername": "[parameters('adminUsername')]",
          "linuxConfiguration": {
            "disablePasswordAuthentication": true,
            "ssh": {
              "publicKeys": [
                {
                  "keyData": "[variables('keyData_var')]",
                  "path": "[format('/home/{0}/.ssh/authorized_keys', parameters('adminUsername'))]"
                }
              ]
            }
          }
        }
      },
      "dependsOn": [
        "[resourceId('Microsoft.ManagedIdentity/userAssignedIdentities', format('{0}-identity', resourceGroup().name))]",
        "[resourceId('Microsoft.Network/networkInterfaces', variables('networkInterfaceName'))]"
      ]
    }
  ],
  "outputs": {
    "adminUsername": {
      "type": "string",
      "value": "[parameters('adminUsername')]"
    },
    "hostname": {
      "type": "string",
      "value": "[reference(resourceId('Microsoft.Network/publicIPAddresses', variables('publicIPAddressName')), '2023-11-01').dnsSettings.fqdn]"
    },
    "sshCommand": {
      "type": "string",
      "value": "[format('ssh {0}@{1}', parameters('adminUsername'), reference(resourceId('Microsoft.Network/publicIPAddresses', variables('publicIPAddressName')), '2023-11-01').dnsSettings.fqdn)]"
    },
    "authUrlCommand": {
      "type": "string",
      "metadata": {
        "description": "Fetch the Tailscale login URL when deployed without an auth key."
      },
      "value": "[format('az vm run-command invoke --resource-group {0} --name {1} --command-id RunShellScript --scripts \"tailscale-authurl\" --query \"value[0].message\" -o tsv', resourceGroup().name, parameters('vmName'))]"
    },
    "authUrlBootLogCommand": {
      "type": "string",
      "metadata": {
        "description": "Same URL via the serial console, no in-VM agent required."
      },
      "value": "[format('az vm boot-diagnostics get-boot-log --resource-group {0} --name {1} -o tsv | grep -o \"https://login.tailscale.com/a/[a-z0-9]*\" | tail -1', resourceGroup().name, parameters('vmName'))]"
    }
  }
}